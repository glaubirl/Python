'''Sorteie 20 inteiros entre 1 e 100 numa lista. Armazene os números pares na lista PAR e os
números ímpares na lista IMPAR. Imprima as três listas.'''

import random

lista = []
par = []
impar = []

for x in range(20):
    lista.append(random.randint(1, 100))

for x in lista:
    if x % 2 == 0:
        par.append(x)
    else:
        impar.append(x)

#print("Lista lista: %d\nLista par: %d\nLista impar: %d" %(lista, par, impar))  #Dá não :')

print('Lista lista: ', lista)
print('Lista par: ', par)
print('Lista impar: ', impar)
